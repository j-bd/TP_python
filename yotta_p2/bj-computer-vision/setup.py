from setuptools import find_packages, setup

setup(
    name='face_detection',
    packages=find_packages(),
    version='1.0.0',
    description='Code to detect masked people.',
    author='Benedicte, Jerome (bj-team)',
)