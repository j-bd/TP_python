from setuptools import find_packages, setup

setup(
    name='universe_vigeo_merging',
    packages=find_packages(),
    version='1.0.0',
    description='Code to merge data.',
    author='Jerome Bohard (Quantmetry)',
)
